
name: tester
type: plugin
version: 0.1.1+dev
description: Unit Testing Framework

plugins: [Doc, Hub]

deps:
  jphp-core: '*'
  jphp-json-ext: '*'

sources:
- src
  
plugin:
  list: [tester\TesterPlugin]
  sources:
  - src-plugin

config:
  ignore: ['/.git/**', '/.idea/**', '/examples/**', '/tests/**']
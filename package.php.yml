
name: tester
type: plugin
version: 1.1.0+dev
description: Unit Testing Framework

plugins: [Doc, Hub]

deps:
  jphp-runtime: '*'
  jphp-json-ext: '*'

sources:
- src
  
plugin:
  list: [tester\TesterPlugin]
  sources:
  - src-plugin

config:
  ignore: ['/.git/**', '/.idea/**', '/examples/**', '/tests/**']